{{define "title"}}
Messages
{{end}}

{{define "content"}}

<div class="row p-2">
   <div class="col">
      <div class="card ">
         <div class="card-header">
            <p class="h5">
               Messages</p>
         </div>
         <div class="card-body">
            {{if .Messages}}
            {{range .Messages}}
            <p>{{.}}</p>
            {{end}}
            {{end}}
         </div>
      </div>
   </div>
</div>

{{end}}